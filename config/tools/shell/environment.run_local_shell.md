
<shell-environment>
## Shell environment

Flat file tree — all files live in root. No subdirectories, no `cd`, no `../`. Paths like `/`, `.`, `./` all refer to the same place.

The shell is read-only. To create, rename, or remove files, use the dedicated file tools.
</shell-environment>
